[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile.freqtrade"

[deploy]
startCommand = "freqtrade trade --config /freqtrade/user_data/config.json --strategy ChannelStrategyV1"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
